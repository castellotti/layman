#!/usr/bin/env bash
# Layman hook for Cline — forwards TaskComplete to Layman server (async, 5s timeout)
curl -s -m 5 -X POST "__LAYMAN_URL__/hooks/cline/TaskComplete" \
  -H "Content-Type: application/json" -d @- > /dev/null 2>&1
